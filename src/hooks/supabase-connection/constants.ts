
export const CONNECTION_CONSTANTS = {
  MAX_RECONNECT_ATTEMPTS: 15,
  BASE_RECONNECT_DELAY: 3000,
  HEARTBEAT_TIMEOUT: 60000,
  HEARTBEAT_INTERVAL: 30000,
  PRESENCE_VALIDATION_DELAY: 5000,
  SESSION_CHECK_INTERVAL: 120000
} as const;
